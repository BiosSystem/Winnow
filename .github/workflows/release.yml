name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Get version from tag
        id: version
        shell: pwsh
        run: |
          $tag = "${{ github.ref_name }}"
          $ver = $tag.TrimStart('v')
          echo "VERSION=$ver" >> $env:GITHUB_OUTPUT
          echo "TAG=$tag" >> $env:GITHUB_OUTPUT

      - name: Build standalone script
        shell: pwsh
        run: |
          .\build.ps1 -OutputFile "Winnow-Standalone.ps1"
          Write-Host "Standalone script built successfully."

      - name: Create release ZIP
        shell: pwsh
        run: |
          $ver = "${{ steps.version.outputs.VERSION }}"
          $zipName = "Winnow-v$ver.zip"
          $items = @(
            "Assets",
            "Config",
            "Regfiles",
            "Schemas",
            "Scripts",
            "Winnow.ps1",
            "Run.bat",
            "README.md",
            "CHANGELOG.md",
            "CONTRIBUTING.md",
            "CODE_OF_CONDUCT.md",
            "CREDITS.md",
            "SECURITY.md",
            "UPSTREAM.md",
            "LICENSE"
          )
          $itemPaths = $items | ForEach-Object { Join-Path $PWD $_ } | Where-Object { Test-Path $_ }
          Compress-Archive -Path $itemPaths -DestinationPath $zipName -Force
          Write-Host "Created $zipName"

      - name: Generate release notes from CHANGELOG
        id: release_notes
        shell: pwsh
        run: |
          $content = Get-Content CHANGELOG.md -Raw
          # Extract section for this version
          $ver = "${{ steps.version.outputs.VERSION }}"
          if ($content -match "(?s)## \[$ver\].*?(?=\n## \[|\Z)") {
            $notes = $Matches[0].Trim()
          } else {
            $notes = "See CHANGELOG.md for details."
          }
          # Write to file to avoid multiline env var issues
          Set-Content -Path release_notes.md -Value $notes
          Write-Host "Release notes extracted."

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.version.outputs.TAG }}
          name: Winnow v${{ steps.version.outputs.VERSION }}
          body_path: release_notes.md
          draft: false
          prerelease: false
          files: |
            Winnow-v${{ steps.version.outputs.VERSION }}.zip
            Winnow-Standalone.ps1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
